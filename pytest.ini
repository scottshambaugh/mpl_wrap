[pytest]
filterwarnings =
    ignore::DeprecationWarning
    # shapely raises this from inside cartopy while it projects the NaN-broken
    # polylines mpl_wrap produces at each seam, so no call site can catch it.
    ignore:invalid value encountered in linestrings:RuntimeWarning
