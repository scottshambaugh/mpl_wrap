[pytest]
filterwarnings =
    ignore::DeprecationWarning
    # mpl_wrap filters this cartopy warning at runtime, but pytest restores the
    # filters after each test, so tests that draw on a GeoAxes without going
    # through a helper still see it.
    ignore:invalid value encountered in linestrings:RuntimeWarning
